pipeline{
    agent any
    parameters{
        string(name: 'fname' , default: 'kamal', description: '')
    }
    stages{
        stage:('one') {
            steps {
                echo '${params.fname}'
            }
        }
    }
        
}
